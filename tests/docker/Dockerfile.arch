# Arch Linux test container for SoulOfNix
# Note: Arch Linux images are only available for amd64, use platform emulation on ARM
FROM --platform=linux/amd64 archlinux:latest

# Install basic dependencies
RUN pacman -Syu --noconfirm && \
    pacman -S --noconfirm \
    bash \
    curl \
    git \
    xz \
    sudo \
    && pacman -Scc --noconfirm

# Create test user
RUN useradd -m -s /bin/bash testuser && \
    echo "testuser ALL=(ALL) NOPASSWD:ALL" >> /etc/sudoers

# Copy SoulOfNix files
COPY --chown=testuser:testuser . /home/testuser/SoulOfNix/

# Switch to test user
USER testuser
WORKDIR /home/testuser/SoulOfNix

# Run test script from inside SoulOfNix directory
CMD ["./tests/test-install.sh"]
